# Solana Live Provider

Solana uses the same transport abstraction with Solana-native RPC method names and network metadata. Account, program, token, NFT, transaction, block, slot, and metadata roles are represented. The adapter preserves signatures/network provenance and emits canonical observations without analytics or indexing.
