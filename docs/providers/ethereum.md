# Ethereum Live Provider

Ethereum uses a configurable JSON-RPC connector with chain ID, optional API key, timeout, and injected transport. Block, transaction, wallet, token, contract, NFT, log, and balance roles share the provider contract. ERC20, ERC721, ERC1155, receipts, logs, blocks, transactions, and accounts are supported as provider payload shapes; adapters create canonical observations without indexing or analytics.
