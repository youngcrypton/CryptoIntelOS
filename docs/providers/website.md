# Website Live Provider

The Website provider exposes an HTTP connector and fetcher with user-agent, timeout, and redirect configuration. Robots, sitemap, metadata, canonical URL, content type, links, assets, and documents are transport/parser responsibilities; semantic inference is excluded. The adapter emits canonical observations only.
