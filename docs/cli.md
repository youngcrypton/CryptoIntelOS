# CLI

Run `python cryptointel.py project example --json` or `python -m src.cli website https://example.test --pretty`. Commands are `github`, `website`, `wallet`, and `project`. Each accepts `--json`, `--pretty`, `--trace`, `--debug`, and `--provider`; output includes execution state, project identifier, confidence, graph nodes, and traceability.
